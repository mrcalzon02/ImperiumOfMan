name: Legacy Package Validator (Manual Only)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired-validator:
    runs-on: ubuntu-latest
    steps:
      - name: Explain replacement workflow
        run: |
          echo "This automatic validator was retired after repeated failures."
          echo "Live-test releases are now published by publish-live-test-release.yml."
